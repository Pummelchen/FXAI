# MQL5 FXDataEngine To Swift Port Plan

This plan tracks the remaining non-tensor MQL5 FXDataEngine surface that must move into the repository-root Swift `FXDataEngine` package before the legacy `FXAI/FXDataEngine` tree can be removed.

## Verified Baseline

- Legacy non-tensor surface checked: `FXAI/FXDataEngine`, excluding `TensorCore` and `Tests/TensorCore`.
- Remaining non-tensor MQL5 files: 120 files, about 53k lines.
- Current Swift package surface: OHLCV market contracts, volume-aware feature contracts, normalization payloads, plugin request DTOs, ML backend descriptors, and Metal capability descriptors.
- Explicit exclusion: old MQL5 `TensorCore` is not part of this Swift data-engine parity gate. Tensor training and inference move to FXPlugins through PyTorch or TensorFlow backends.

## Migration Boundaries

- `FXDataEngine` owns canonical M1 OHLCV input, feature construction, normalization, plugin payload contracts, offline context state, runtime policy metadata, and non-trading governance artifacts.
- `FXPlugins` owns model execution, plugin-specific training/inference, PyTorch/TensorFlow process bridges, and plugin state checkpoints.
- `FXBacktest` owns simulated broker execution, parameter sweeps, CPU/Metal pass scheduling, ledger output, and backtest result persistence.
- Live MT5 order APIs are not copied into Swift. Any old trade-execution logic that remains relevant must become deterministic offline policy/risk inputs for FXBacktest or an explicit future live-agent contract.

## Port Workstreams

| Phase | Legacy MQL5 Area | Swift Target | Scope | Exit Gate |
| --- | --- | --- | --- | --- |
| 1 | `Engine/Core`, `feature_registry`, `feature_math`, `feature_build`, `feature_norm` | `FXDataEngine` | Complete OHLCV feature math, MTF alignment helpers, volume replacements for old spread/cost slots, fitted/rolling normalization helpers. | Golden tests on feature indexes, MTF windows, volume on/off behavior, and normalization methods. |
| 2 | `API`, `API/Context`, `API/Contract` | `FXDataEngine` and `FXPlugins` | Plugin manifest/context/replay/quality/persistence DTOs; keep tensor bridge as PyTorch/TensorFlow descriptor only. | Plugin contract validation tests and fixture parity against MQL defaults. |
| 3 | `Runtime/ControlPlane`, `runtime_artifacts` | `FXDataEngine` | Safe token/path helpers, TSV profile parsing, deployment/supervisor/router profiles, snapshot/aggregate DTOs, artifact manifests. | TSV parser tests, clamping/default tests, manifest path tests. |
| 4 | `market_data_gateway`, `data_io`, `data_align`, `data_pipeline`, `engine_samples` | `FXDataEngine` with `FXDatabase` | FXDatabase-backed market loading, M1-only OHLCV assumptions, MTF resampling/alignment, sample windows, labels, context symbols. | No spread/tick dependency; fixture windows match expected labels and volume flags. |
| 5 | `event_macro`, `runtime_calendar_cache`, factor/context state | `FXDataEngine` | Offline macro/news/rates/context state readers and feature adapters. | Stale/unavailable states fail closed and produce deterministic feature payloads. |
| 6 | `Services` probes | `FXDatabase` or `FXDataEngine` readers | Convert MT5 service collectors into offline provider inputs or read-only context-state parsers. | No MT5 runtime dependency; service data can be replayed from files/API fixtures. |
| 7 | `Runtime` model/router/policy stages | `FXDataEngine` | Prediction orchestration, adaptive router, dynamic ensemble, probabilistic calibration, execution-quality policy stage, signal finalize. | Stage-by-stage tests with mock plugins and deterministic policy outputs. |
| 8 | `Runtime/Trade`, `Core/core_broker_execution` | `FXBacktest` plus `FXDataEngine` policy DTOs | Convert exposure, pair-network, risk, execution-quality and lifecycle logic into offline simulation inputs. | FXBacktest integration tests verify ledger/risk behavior without live order APIs. |
| 9 | `Lifecycle`, `Warmup`, `engine_lifecycle`, `engine_warmup` | `FXDataEngine` and agents | Bootstrap, compliance, context symbol selection, reset/recovery, warmup scoring, portfolio warmup. | Lifecycle tests cover symbol/session/regime reset and warmup readiness. |
| 10 | `Tests`, audit/scoring/scenario harness | `FXDataEngine/Tests`, `FXBacktest/Tests` | Rebuild MQL audit suites as Swift fixtures and regression tests. | Every retained legacy behavior has a Swift test or an explicit retirement note. |

## Current Gaps By Module

- `Engine/Core`: partially ported. Core constants, feature groups, plugin families, data request basics, context symbol filtering, timeframe need/lag helpers, aligned-index maps, context aggregate arrays, context aggregate feature-slot adapters, feature schema, normalization concepts, model-context capacity/span helpers, analog-memory/hierarchical signal scoring, regime graph transition scoring, runtime performance tracking, broker execution profile/cost/replay helpers, and volume-aware execution trace stats exist in Swift; stateful broker execution memory and live order coupling remain FXBacktest/live-agent migration work.
- `Engine` root: partially ported. Feature registry/build/norm exist only as a first slice; legacy as-series feature math primitives, deterministic feature-build session/carry/drift helpers, training random/hyperparameter descriptors, routed model-state resolution, and prepared-sample window shaping are ported for parity checks; runtime artifact text manifests, binary runtime artifact header/envelope persistence, typed v15 payload section ownership/layout, deterministic replay reservoir state, conformal calibration state, normalization window/config state, normalization rolling history state, and normalization fit state are ported; M1 OHLCV training-sample preparation now covers ascending triple-barrier labels, plugin train payloads, deterministic range-based training datasets, deterministic horizon configuration helpers, FXDatabase M1 history loading, canonical ascending bar window helpers, M1-derived MTF bar shaping/alignment, context-symbol FXDatabase range request policy, offline macro-event TSV/features/state adapters, calendar-cache news gate readers, prepared-input factor context scoring, deterministic system-health aggregation, prepared-state meta calibration/horizon/reliability/policy/stacker helpers, reliability pending-outcome compaction, stack/policy/horizon pending replay queues, bounded replay outcome targets, and meta-artifact path/header/save-throttle planning. Runtime/plugin-state integration remains.
- `Runtime/ControlPlane`: partially ported. Swift now has profile/snapshot DTOs, safe path helpers, TSV parsing, adaptive/student router weight helpers, portfolio supervisor defaults, supervisor service/command state parsing, freshness checks, command direction/budget helpers, file-backed profile loaders, peer aggregate scoring, and stale snapshot pruning.
- `Runtime/Trade`: partially ported. Pure symbol/currency exposure helpers, Pair Network config/exposure math, risk-policy admission/sizing/overlay DTOs, lifecycle recommendation math, and deterministic execution-plan construction are in FXDataEngine; remaining broker position/order scanning, lifecycle execution, peer discovery, broker lot normalization, order-check/send/cancel/modify effects, and live exposure source collection must be split between FXBacktest simulation and future live-agent contracts.
- `Runtime` stages: partially ported. Time-context helpers, runtime-cycle entrypoint planning, router model-cap pressure logic, final signal reason/intensity math, runtime signal cache/projection state, deterministic feature-pipeline/transfer handoff planning, runtime model-router selection/filtering, prepared policy-stage arbitration/posture gates, execution-quality config/tier policy support, and probabilistic-calibration prepared-state policy support are available as deterministic Swift contracts; broader model/policy orchestration still needs plugin mockability before real PyTorch/TensorFlow backends.
- `Lifecycle` and `Warmup`: partially ported. Lifecycle context-symbol category/scoring helpers, lifecycle compliance validation/clamp/window/distance primitives, lifecycle reset planning, lifecycle bootstrap validation planning, Warmup execution planning, transfer/normalization split helpers, adaptive EV thresholds, serious-native epoch/block budgets, block-batch planning, sample caps, portfolio diagnostics, curriculum-priority scoring, Warmup bucket scoring, and portfolio-objective proxy math are in Swift; remaining provider-backed context discovery, warmup loops, plugin pretraining, plugin predict/self-test execution harnesses, and agent orchestration still need conversion.
- `Services`: partially ported. NewsPulse, Rates Engine, Cross Asset, Microstructure, and Execution Quality state readers are available in Swift, including calendar-cache fallback for NewsPulse; remaining MT5 service collectors should become offline data-provider inputs or deterministic readers.
- `Tests`: partially ported. Swift now has the first audit-scoring primitives for prediction deltas, fold counters/scores, deflated-Sharpe proxy, and walk-forward aggregation; the broader MQL audit scenario runners and report writers still need conversion.

## Port Progress

- Done: control-plane foundations: deployment, portfolio supervisor, student router, adaptive router, supervisor service, supervisor command, snapshot DTOs, MQL-style TSV parsing, clamping/defaults, safe paths, and direction/budget helper behavior.
- Done: runtime artifact foundations: runtime artifact paths, version/header contract, runtime stage names, persistence coverage manifest row logic, feature registry manifest rows, feature clip bounds, and stateful checkpoint promotion readiness logic.
- Done: file-backed control-plane repositories and snapshot aggregation with MQL-style stale snapshot pruning.
- Done: runtime artifact text manifest writers for persistence coverage, feature registry, macro dataset stats, runtime performance rows, and shadow fleet ledger rows.
- Done: DataCore request/context helper slice: context symbol normalization/de-duplication, legacy higher-timeframe need/lag calculations, ascending M1 aligned-index maps, and deterministic context aggregate arrays.
- Done: FeatureCore now consumes DataCore context aggregates for feature slots 10-12 and 50-65, including MQL-style raw volatility scaling, signed up-ratio mapping, shared adapter mapping, and previous-frame payload parity.
- Done: binary runtime artifact envelope slice: MQL-order header encode/decode, payload preservation, repository read/write/file-size helpers, and the legacy dirty-save throttle.
- Done: first sample-preparation slice: no-spread M1 OHLCV triple-barrier labels on ascending Swift data, sample quality/weight/path-risk targets, and `TrainRequestV4` payload preparation.
- Done: deterministic training dataset range builder for repeated offline `TrainRequestV4` payload generation from one `MarketUniverse`.
- Done: model-context capacity helpers: horizon clamping, symbol/horizon model scale, sequence/batch/tree span defaults, move EMA state, and three-way split branching.
- Done: deterministic horizon configuration helpers: MQL-style horizon list parsing, configured-horizon slot lookup, max horizon resolution, safe array reads, positive int means, and no-spread static regime bucketing.
- Done: analog-memory core: foundation/student/analog/hierarchical signal DTOs, legacy analog feature vector mapping, no-spread ring-buffer memory updates, weighted analog queries, horizon buckets, and hierarchical score blending.
- Done: regime graph core: transition/dwell state, macro alignment EMA, feedback edge/quality EMA, predicted regime selection, and query score outputs.
- Done: runtime performance core: stage/plugin latency EMA, max/observation tracking, plugin working-set estimates, budget pressure, and performance manifest row generation.
- Done: typed runtime artifact section materialization: v15 binary payload section offsets/byte counts, owner mapping across FXDataEngine/FXPlugins/FXBacktest, prepared replay sample codec, feature-drift codec, and analog-memory codec.
- Done: deterministic replay reservoir state: no-spread replay priority, bucket-balanced insertion/eviction, duplicate time guards, outcome boosts, deterministic replay sample selection, optional analog-memory update, and legacy section binary codec.
- Done: conformal calibration state: score quantiles, pending prediction queue, no-spread realized-outcome updates, prediction adjustment, and legacy conformal section binary codec.
- Done: normalization window/config state: target default windows, per-feature group window building, legacy/runtime config mirrors, version bumps, and binary codecs.
- Done: normalization rolling history state: method policy flags, per-method/per-feature ring buffers, rewind/config-change resets, newest-first history reads, and binary codec.
- Done: normalization fit state: fallback stats, fitted min/max/mean/std/robust quantiles, Yeo-Johnson grid stats, quantile-to-normal lookup, and binary codec.
- Done: meta-support normalization helper slice: deterministic bar sampling, percent sampling gates, shadow-cadence checks, canonical AI model IDs, normalization method sanitization, and MQL-style method candidate ordering are ported. Routed model normalization-bank lookup still belongs with runtime/plugin state integration.
- Done: meta-calibration helper slice: portfolio objective/factor math, plugin route action utility, route-cell online updates, route factor, prune gates, and prepared model meta-score calculation are ported without live broker or spread-based regime classifier inputs.
- Done: horizon OOF prior helper slice: `meta_horizon.mqh` out-of-fold horizon prior EMA updates, prior scoring, and trade-gate prior math are ported as deterministic Swift state. Legacy horizon feature inputs that depended on live execution fields stay deferred until the offline FXBacktest routing contract replaces them.
- Done: horizon policy network slice: `meta_horizon.mqh` tanh scorer, fixed 48-feature/16-hidden policy state, prediction, and online reward update math are ported for prepared feature vectors. Feature construction remains deferred because the legacy builder contains spread fields that must be replaced by offline FXBacktest/FXDataEngine cost inputs.
- Done: stack-router/helper-network slice: `meta_stacker.mqh` model-list lookup, 84-slot stack feature construction with prepared max-horizon input, stack portfolio/routing objectives, context trust, action utility, action-cell online observation updates, probability blending, heuristic stack prediction, 28-hidden learned stack prediction/update, OOF-aware trade-gate prediction, and 16-hidden trade-gate online update math are ported. Runtime queue consumption orchestration remains deferred until FXBacktest owns the offline outcome contract.
- Done: reliability pending-outcome resolution slice: deterministic queue compaction behavior from `meta_reliability.mqh` is ported, including current-signal sequence gates, aged-entry detection by horizon, prediction/future index calculation, history-availability evaluation flags, legacy drop-on-aged semantics, and retained queue rebuilding. Actual label construction, reliability/calibration/performance updates, replay-priority boosts, and provider refresh remain FXDataEngine/FXBacktest orchestration work.
- Done: meta pending replay queue slice: deterministic ring-buffer behavior shared by `meta_stacker.mqh`, `meta_policy.mqh`, and `meta_horizon.mqh` is ported, including typed stack/policy/horizon feature-vector widths, same-sequence replacement, overflow eviction, aged-entry detection, history-availability flags, drop-on-aged semantics, and retained queue rebuilding. Runtime queue orchestration remains future agent work.
- Done: meta pending replay outcome slice: bounded M1 OHLCV triple-barrier labeling now builds stack, policy, horizon-policy, stack-router, trade-gate, and regime-graph update targets from aged pending replay actions using prepared price-cost inputs. The helper avoids future-data leakage by evaluating only through the resolved horizon/current bar, while runtime/plugin queue orchestration remains future agent work.
- Done: meta persistence foundation slice: `meta_persistence.mqh` artifact path sanitization, version-8 dimension header order, compatibility mismatch reporting, and dirty-save throttle planning are ported. Full combined meta-state body serialization remains future repository work because stack/policy/horizon/reliability states are now Swift-owned structs rather than MQL globals.
- Done: feature math helper slice: `feature_math.mqh` pure as-series math helpers are ported for direct parity checks, including safe returns, normalized slope, expected move estimators, rolling return stats, SMA/EMA, candle geometry, QSD-EMA, RSI, ATR, Parkinson/Rogers-Satchell/Garman-Klass volatility, rolling median/MAD, Kalman estimate, and Ehlers super smoother. FeatureCore still uses ascending M1 OHLCV wrappers and can now be tightened against these helpers in later feature-build parity work.
- Done: feature-build support slice: deterministic `feature_build.mqh` helpers for cyclic hour pulses, rollover/session transition/session overlap scores, prepared carry/swap feature replacement, and feature-family drift are ported. FeatureCore time slots now use the legacy signed weekday/hour/minute and session formulas, while volume slots remain the no-spread replacement path.
- Done: training hyperparameter descriptor slice: deterministic `engine_training.mqh` random seed/range generation, threshold-pair sampling, base hyperparameter clamping, model-specific defaults, and model-specific sampling ranges are ported as Swift descriptors. Actual model fitting and tensor execution remain FXPlugins responsibilities through PyTorch/TensorFlow backends.
- Done: routed model-state resolution slice: `engine_training.mqh`/`meta_support.mqh` global, horizon, and regime-horizon precedence is ported for model hyperparameters, normalization methods, and threshold pairs, including horizon-edge threshold adjustment. This is state-resolution only; runtime mutation and plugin training remain orchestration work.
- Done: prepared sample window slice: `engine_training.mqh` prepared-sample window shaping is ported for ascending Swift samples, including requested-length clamping, older-sample row order, stop-on-invalid behavior, routed normalization-cache request planning, routed cache lookup, and cached prepared-window shaping. Cache materialization/fitting and plugin training loops remain orchestration work once FXPlugins are wired.
- Remaining in phase 3: no known non-tensor runtime artifact state gaps; tensor/plugin model state remains owned by FXPlugins, and broker execution state moves to FXBacktest.
- Done: first FXDatabase market gateway slice: async M1 OHLCV loader, source-origin/provider-symbol request mapping for non-MT5 providers, canonical ascending bar index/slice/window helpers, and volume-preserving response conversion.
- Done: M1-derived MTF bar shaping: strict full-bucket resampling for M5/M15/M30/H1, optional partial buckets, volume summing, OHLC invariant validation, and alignment maps back to canonical M1 time.
- Done: context-symbol FXDatabase range policy: primary-first symbol ordering, normalized/de-duplicated context symbols, per-symbol provider and digits expectations, and shared UTC range contracts.
- Remaining in phase 4: no known local data-gateway gaps; retained sample-window parity fixtures should continue expanding under phase 10 audit-suite migration.
- Done: offline macro-event dataset slice: legacy TSV parser, macro dataset stats/leakage policy, event-window features, macro pressure state, 20-slot macro feature-vector overlay, and window activity scoring.
- Done: runtime calendar-cache slice: state/feed TSV parsers, symbol-pair event gating, stale detection, max-six reason de-duplication, and legacy `BLOCK`/`CAUTION`/`SAFE` policy outputs.
- Done: runtime factor-context slice: trend/carry/policy/value/commodity formulas, base-minus-quote pair composition, bias/alignment/rationale outputs, and explicit prepared-input replacements for MT5 D1/swap/symbol APIs. Swift adds a neutral USD fallback so USD pairs stay usable offline.
- Done: runtime system-health aggregation: prepared-input health states for news, rates, cross-asset, microstructure, execution quality, calendar cache, and factor context now preserve legacy scoring, reason CSV, readiness/staleness flags, execution `stale || data_stale` handling, and posture thresholds. MT5 cycle/order invariant repair from `runtime_system_health.mqh` is intentionally not ported to FXDataEngine because it mutates live broker state; any retained behavior belongs in FXBacktest simulation policy or future live-agent contracts.
- Remaining in phase 5: lifecycle wiring that consumes macro/calendar/factor/system-health availability and broader runtime policy integration.
- Done: runtime time/router/finalize micro-slice: `runtime_time_service.mqh` reset/build/offset conversion/summary behavior is ported to prepared Swift time inputs, `runtime_router_stage.mqh` performance-budget model capping is ported against `RuntimePerformanceState`, and `runtime_signal_finalize.mqh` final reason priority plus signal-intensity math are ported without MT5 global snapshot side effects.
- Done: NewsPulse service-state reader: `runtime_trade_newspulse.mqh` symbol-map parsing, supported-currency heuristic pair IDs, flat pair snapshot parsing, max-six reason de-duplication, freshness/stale handling, value clamps, and calendar-cache fallback are ported as pure Swift inputs.
- Done: Rates Engine service-state reader: `runtime_trade_rates_engine.mqh` symbol-map parsing, NewsPulse pair-id fallback, flat pair snapshot parsing, max-six reason de-duplication, freshness/stale handling, value clamps, and string defaults are ported as pure Swift inputs.
- Done: Cross Asset service-state reader: `runtime_trade_cross_asset_state.mqh` symbol-map parsing, NewsPulse pair-id fallback, global score plus pair snapshot parsing, max-eight reason de-duplication, and legacy stale-on-missing-clock/generated-time behavior are ported as pure Swift inputs.
- Done: Microstructure service-state reader: `runtime_trade_microstructure.mqh` symbol-map parsing, NewsPulse pair-id fallback, flat pair snapshot parsing, max-six reason de-duplication, freshness/stale handling, value clamps, string defaults, and un-clamped caution overrides are ported as pure Swift inputs. Legacy spread/tick fields remain isolated to this service-state contract and are not part of the M1 OHLCV market-data contract.
- Done: Execution Quality service-state reader: `runtime_trade_execution_quality.mqh` safe-token runtime path building, per-symbol key/value TSV parsing, max-twelve reason CSV splitting, freshness stale override, separate data-stale handling for system health, and un-clamped spread/slippage/caution outputs are ported as pure Swift inputs. Legacy spread/slippage fields remain isolated to this service-state contract and are not part of the M1 OHLCV market-data contract.
- Done: Execution Quality runtime policy slice: `runtime_execution_quality_stage.mqh` config defaults, legacy TSV config parsing, memory tier parsing, tier hierarchy matching/selection, fallback tier behavior, session-thinness scoring, prepared-state runtime score application, per-symbol state TSV serialization, and history NDJSON appends are ported with Swift cost-oriented inputs for legacy spread config/state keys.
- Done: Probability Calibration runtime policy slice: `runtime_prob_calibration_stage.mqh` config defaults, legacy TSV config parsing, memory tier parsing, tier hierarchy matching/selection, fallback tier behavior, prepared-state probability calibration, move distribution scaling, uncertainty/cost/risk penalties, abstain reason priority, per-symbol state TSV serialization, and history NDJSON appends are ported with Swift cost-oriented inputs. The legacy `spread_cost_points` artifact key remains only for existing GUI/runtime reader compatibility.
- Done: Adaptive Router runtime slice: `runtime_adaptive_router_stage.mqh` regime/session labels, prepared M1 OHLCV regime-state build, plugin suitability/status/posture math, abstain-bias calculation, per-symbol state TSV serialization, and history NDJSON appends are ported with Swift price-cost inputs. The legacy `spread_regime` artifact key remains only for existing GUI/runtime reader compatibility.
- Done: Dynamic Ensemble runtime slice: `runtime_dynamic_ensemble_stage.mqh` config defaults and TSV parsing, family compatibility tables, prepared plugin-record trust scoring, adaptive-router handoff handling, normalized weight capping, ensemble quality/posture/probability/action outputs, and per-symbol state/history artifacts are ported with Swift price-cost inputs. Plugin execution and live policy-global mutation remain FXPlugins/FXBacktest integration work.
- Done: Runtime signal-state cache slice: `runtime_signal_state.mqh` reset defaults, signal/policy/control-plane/adaptive-router/dynamic-ensemble/execution-quality/probability-calibration cache fields, stage projection helpers, idle/control-plane snapshot row generation, and file-backed local snapshot writes are ported as deterministic Swift contracts. MT5 global capture/restore side effects are intentionally replaced by prepared-state projections; price-cost naming is used for new Swift fields, with legacy spread names confined to already-existing service-state/artifact compatibility surfaces.
- Done: Runtime feature-pipeline and transfer handoff slice: deterministic pieces of `runtime_feature_pipeline_block.mqh` and `runtime_transfer_stage_block.mqh` are ported, including needed-bar/alignment planning, init/online/shadow window planning, dynamic context strength/quality math, price-cost horizon-policy feature construction, prepared routed-horizon scoring with regime/model/policy/prior inputs, model input vector construction, and shared transfer window shaping. Data-provider refresh, feature-frame building, replay insertion, foundation/student tensor execution, and live control-plane aggregate reads remain split across existing FXDataEngine helpers, FXPlugins, and FXBacktest integration work.
- Done: Runtime model-router slice: deterministic active-model selection from `runtime_model_stage_block.mqh` is ported, including single-model availability gating, ensemble top-k ranking, exploration arm selection with cold-start bonus, fallback by vote weight when all models are pruned, runtime performance model caps, student-router allow/min-weight filtering, adaptive-router suitability/status suppression, route cutoffs, and routed meta weights. Plugin initialization, normalization cache construction, training, inference, pending tensor updates, and prediction aggregation remain FXPlugins/FXBacktest orchestration work.
- Done: Runtime policy-stage arbitration slice: deterministic decision gates from `runtime_policy_stage_block.mqh` are ported, including hierarchy/execution viability blocks, policy no-trade and macro-quality floors, skip-vote blocks, policy-gate floors, buy/sell policy-score decisions, conservative EV fallback decisions, chosen-edge output, dynamic-ensemble posture mutations, dynamic final-score contradiction blocks, and adaptive-router posture mutations. Execution-quality/probability-calibration application and runtime artifact writing remain handled by their dedicated Swift helpers plus future FXPlugins/FXBacktest orchestration.
- Done: Runtime cycle entrypoint planning slice: deterministic shell behavior from `engine_runtime.mqh` is ported, including runtime input clamps, AI type fallback, ensemble decision-key calculation, threshold/EV sanitization, plugin-readiness fail-closed handling, symbol-reset planning, warmup gate ordering, signal-bar fail-closed handling, and cached-signal restore decisions. Feature refresh, plugin training/inference, runtime global mutation, and stage execution remain orchestration work for FXPlugins, FXBacktest, and agents.
- Done: Runtime exposure pure helper slice: `runtime_trade_exposure.mqh` symbol-leg parsing, shared-currency detection, correlation exposure weights, runtime base/quote prefix helpers, directional exposure signs, and directional cluster alignment are ported without MT5 position/order APIs. Live managed-position, pending-order, and money-per-point helpers remain FXBacktest/live-agent work.
- Done: Pair Network pure helper slice: `runtime_trade_pair_network.mqh` defaults, config/status TSV parsing, safe-token runtime paths, decision labels, decision-state reason handling, currency profiles, base-minus-quote factor exposure, exposure CSV formatting, currency/factor concentration metrics, structural-overlap scoring, macro-fit scoring, quality-score blending, per-symbol state TSV serialization, and history NDJSON appends are ported without live peer discovery or order/position APIs. Live peer collection remains FXBacktest/live-agent work.
- Done: Risk policy foundational slice: `runtime_trade_risk.mqh` and `runtime_trade_exposure.mqh` kill-switch reason priority, prepared-state admission gates, factor/system-health/macro floors, service block/caution floors, Pair Network auto-apply blocks/reduced-size handoff, conviction blending, estimated risk-points, prepared portfolio-pressure scoring/gating, supervisor/service/command overlay blocks, overlay lot scaling, prepared exposure-cap application, risk-budget lot cap, and fixed/conviction/vol-target lot math before broker normalization are ported as deterministic Swift helpers. Live exposure collection from positions/orders, symbol lot-step normalization, money-per-point discovery, account equity reads, and order filling resolution remain FXBacktest/live-agent work.
- Done: Lifecycle recommendation slice: `runtime_trade_lifecycle.mqh` prepared managed-position state, add/reduce/timeout/tighten/exit probability math, action priority, reduce-fraction sizing, and add-size multiplier recommendation are ported without CloseAll, stop modification, partial close, add-order, or lifecycle action cooldown side effects. Those execution effects remain FXBacktest/live-agent work.
- Done: Broker execution replay slice: `core_broker_execution.mqh` execution profile presets, entry/slippage/fill/deviation math, replay-frame construction, and recent OHLCV trace-stat construction are ported without market-data spread inputs. The trace builder uses volume as the liquidity-stress source when volume is present and neutral liquidity otherwise. Stateful broker event memory and live order APIs remain FXBacktest/live-agent work.
- Done: Runtime trade execution-plan slice: deterministic parts of `runtime_trade_execution.mqh` are ported, including reset defaults, order-type bucketing, price normalization, minimum-distance handling, protective stop/take-profit math, pending-vs-market preference gates, breakout-vs-limit pending order selection, 20-minute pending expiry, and post-normalization pending distance repair. Swift uses a single M1 OHLCV reference price instead of bid/ask/spread, while `OrderCheck`, `OrderSend`, pending cancellation, stop modification, partial close, and lifecycle add-order side effects remain FXBacktest/live-agent responsibilities.
- Done: Warmup scoring primitive slice: `warmup_scoring.mqh` bucket-stat reset/update/score math and `warmup_normalization.mqh` portfolio-objective proxy are ported as deterministic Swift helpers. Warmup trial loops, plugin training, transfer pretraining, and normalization bank selection remain split work for FXDataEngine contracts plus FXPlugins/FXBacktest execution.
- Done: Warmup entrypoint planning slice: `warmup_entrypoint.mqh` warmup sample/loop/epoch/fold/min-trade clamps, base/configured horizon resolution, threshold sanitization, EV/cost clamps, needed-bar calculation, EV lookback clamp, AI hint resolution, transfer cap, and portfolio evaluation cap are ported. Data loading, normalization search, plugin training, transfer pretraining, and artifact saves remain integration work across FXDataEngine, FXPlugins, and FXBacktest.
- Done: Warmup transfer/selection helper slice: `warmup_transfer.mqh`, `warmup_normalization.mqh`, `warmup_scoring.mqh`, `warmup_portfolio.mqh`, and `meta_thresholds.mqh` deterministic transfer universe ordering, transfer sample ranges, reverse-stride sample caps, normalization model anchors, validation/train split math, fold split math, adaptive thresholds, EV signal classing, serious-native epoch budgets, portfolio correlation estimates, primary/transfer weights, and portfolio diagnostic aggregation are ported. The retained loops that train plugins, update stackers, pretrain transfer heads, or load provider data remain FXPlugins/FXBacktest integration work.
- Done: Warmup serious-native parity correction: `engine_training.mqh` serious-native model membership is now mirrored through `AIModelID`, warmup block-span planning, tensor-optimizer block-batch planning, and curriculum priority scoring are available for future plugin-training orchestration using Swift `fillRisk` as the no-spread replacement input.
- Done: Lifecycle context-symbol scoring slice: `lifecycle_context_symbols.mqh` symbol category, category priority, shared-symbol score, session-overlap score, redundancy/persistence/incremental-value math, candidate scoring with prepared liquidity/data-health inputs, score-reference building, curated universe, and raw context-list parsing are ported. MT5 `SymbolSelect`, tick/spread, and rates-copy probes are intentionally replaced by prepared provider inputs.
- Done: Lifecycle compliance primitive slice: `lifecycle_compliance.mqh` prediction validation reasons, stricter compliance output range checks, sequence/horizon clamp rules, synthetic window decay, prediction-distance weights, and deterministic fuzz LCG are ported. Plugin train/predict/reset/persistence compliance harnesses remain FXPlugins/agent work because they execute plugin backends and state checkpoints.
- Done: Lifecycle reset/recovery planning slice: deterministic defaults from `lifecycle_reset_recovery.mqh` are ported as a Swift reset plan, including fail-closed signal/probability/execution-quality defaults, warmup-done resolution, horizon-list parsing, reset-action ordering, optional plugin-reset action, meta/runtime artifact load actions, context utility clearing, normalization-window fallback or reuse, and per-AI trained/last-train reset state. Direct global mutation, plugin object reset execution, artifact loading execution, and model auxiliary reset execution remain orchestration responsibilities.
- Done: Lifecycle bootstrap validation planning slice: deterministic request-building behavior from `lifecycle_bootstrap.mqh` is ported, including feature-registry fail-fast handling, per-plugin manifest validation, dummy feature-vector construction, session-bucket/domain-hash context setup, 5-minute compliance horizon clamping, sequence/window resolution, decayed compliance-window filling, point-value fallback, volume-availability propagation, and normalized predict-request validation. Actual plugin initialization, `Predict`, prediction-output validation, and `SelfTest` execution remain FXPlugins/agent responsibilities.
- Done: Audit scoring foundation slice: `audit_scoring_core.mqh` prediction-delta math, decision selection, session-edge pressure, adversarial weakness scoring with Swift `fillRisk` as the no-spread replacement, and fold counters plus `audit_scoring_metrics.mqh` fold scoring, approximate normal CDF, sample mean/std, deflated-Sharpe proxy, walk-forward aggregation, macro-aware final scenario scores, and issue flags are ported as Swift audit contracts. `audit_scenario_spec.mqh`, `audit_world_plan.mqh`, and pure `audit_context_series.mqh` helpers are also ported: scenario defaults, world-plan TSV overrides, deterministic world hash/session helpers, synthetic OHLCV bar normalization, as-series rolling correlation, chronological reversal, MTF close aggregation, context-series derivation, aggregate context features, and context MTF extras with volume/fill-risk liquidity pressure instead of spread arrays. Full scenario series generation, plugin execution, report writing, and runner orchestration remain phase-10 work.
- Done: Audit utility slice: `audit_utils.mqh` array fallback helpers, positive-int means, audit hard-coded horizon buckets, normalization-method sanitation, slim-runner default hyperparameters, and static regime bucketing are ported as Swift audit utilities. The old spread regime bit is replaced by a prepared liquidity-stress bit so the public audit contract remains M1 OHLCV plus volume/fill-risk.
- Done: Audit report slice: `audit_report.mqh` report header order, row field order, fixed-decimal formatting, average confidence/reliability/move/trend fields, issue-flag output, and TSV document rendering are ported. Scenario generation, plugin execution, and audit runner file orchestration still remain phase-10 work.
- Done: Audit world-plan application slice: deterministic `FXAI_AuditApplyWorldPlanToMarketRates` OHLC transformation logic is ported for reverse-chronological Swift scenario bars. Legacy spread scaling is replaced with fill-risk scaling, and volume is reduced under liquidity stress when source volume is present.
- Done: Audit synthetic scenario generation slice: non-market `FXAI_AuditGenerateScenarioSeries` paths now build deterministic Swift as-series OHLCV, M5/M15/M30/H1 close aggregates and maps, derived context series, and context feature arrays. Market-backed scenario selection, plugin execution, and runner orchestration remain phase-10 work.
- Done: Audit market scenario generation slice: market-backed audit scenarios now accept FXDatabase-style ascending M1 OHLCV, select legacy recent/trend/chop/session/liquidity/macro-proxy windows, apply no-spread world-plan transforms, derive MTF closes from M1, and build volume/fill-risk context features. Plugin execution, adversarial scenario generation, and runner orchestration remain phase-10 work.
- Done: Audit sample/window bridge slice: generated audit scenarios can now be converted into aligned Swift `MarketUniverse` inputs and prepared as validated predict/train payloads with as-series future guards, sequence windows, triple-barrier labels, volume-aware contexts, and execution trace stats. Actual plugin train/predict execution and audit runner orchestration remain phase-10 work.
- Done: Audit protocol runner slice: Swift now has an `FXAIPluginV4`-backed audit scenario loop that prepares samples, validates predict/train payloads, records valid/invalid prediction metrics, trains after evaluation, computes reset/sequence deltas, and finalizes scenario scores. Full file orchestration, adversarial scenario generation, and real FXPlugins backend execution remain phase-10 work.

## Start Order

1. Port `Runtime/ControlPlane` DTOs, safe-token/path helpers, CSV weight utilities, snapshot parsing, and profile defaults.
2. Port `runtime_artifacts` manifest path helpers and feature/persistence manifest DTOs.
3. Finish `Engine/Core` parity for data requests, context-series aggregation, MTF needs/lags, and feature schema helpers.
4. Finish feature math/build/norm parity using M1 OHLCV and volume, with no spread/tick paths.
5. Add FXDatabase-backed sample/window builders and labels.
6. Add runtime stage DTOs and mock-plugin orchestration.
7. Move trade/risk behavior into FXBacktest integration tests.
8. Convert lifecycle/warmup and audit suites.

## Deletion Gate

The MQL5 `FXAI/FXDataEngine` folder can be removed only when each non-tensor file is mapped to one of these outcomes:

- ported to Swift and covered by tests,
- moved to `FXPlugins` as plugin-owned behavior,
- moved to `FXBacktest` as simulation behavior,
- moved to `FXDatabase` as data-provider behavior,
- retired with an explicit rationale in this document.
