FXAI Plugin Reference Upgrade Matrix

Purpose: current estimated reference-fidelity score for each live plugin plus the highest-value next improvements.
Scale: 100% means very close to a strong full reference implementation within pure MQL5 limits. Foundation-style surrogates are naturally capped.

ai_autoformer - 94%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_chronos - 92%
1. Deepen the native retrieval/token forecast path so the surrogate behaves less like a compressed generic transformer.
2. Strengthen sequence-window supervision and horizon-aware uncertainty output.
3. Improve memory/replay curriculum for recurring motifs and walk-forward drift.
4. Use Audit Lab to validate token-memory stability under regime shifts and session boundaries.
5. Further reduce remaining simplifications in the foundation-style inference path where CPU cost stays acceptable.

ai_geodesic - 94%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_lstm - 95%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_lstmg - 96%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_mlp - 95%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_patchtst - 92%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_s4 - 94%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_stmn - 92%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_tcn - 92%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_tft - 93%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_timesfm - 91%
1. Expand native distribution output from window-aware hidden states even further, with stronger horizon-specific uncertainty.
2. Increase explicit sequence usage in training updates so the TimesFM surrogate behaves less like a flat predictor.
3. Strengthen token/codebook realism and calibration under drift-heavy market replay.
4. Add richer Audit Lab oracles for foundation-style surrogate behavior and long-horizon consistency.
5. Reduce remaining surrogate simplifications in the latent forecasting stack where MQL5 still allows it.

ai_trr - 94%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

ai_tst - 93%
1. Use explicit API v4 sequence windows end-to-end in both predict and train paths.
2. Expose fully native distribution outputs with calibrated quantiles, confidence, and reliability.
3. Remove any remaining fallback dependence on shared heuristic move priors in production prediction.
4. Tighten plugin-specific calibration and long-run state-reset behavior under regime/session changes.
5. Add stronger Audit Lab oracle checks for sequence integrity, reset safety, and drift stability.

dist_quantile - 93%
1. Keep all outputs fully native to the model distribution, including calibrated class and move uncertainty.
2. Improve execution-aware calibration under cost, spread shock, and walk-forward regime transitions.
3. Strengthen replay sampling around tail events and low-edge skip decisions.
4. Align Audit Lab scoring with quantile coverage, PIT stability, and tail-risk discipline.
5. Use ablations to optimize which feature groups materially improve quantile sharpness.

lin_enhash - 94%
1. Expose fully native distribution outputs from model-owned uncertainty instead of generic fallbacks.
2. Improve calibration drift handling by session/regime/horizon with stronger plugin-owned banks.
3. Refine replay hard-sample selection to focus on costly false positives and missed moves.
4. Add richer interaction uncertainty or sparse-feature reliability telemetry for the meta layer.
5. Use Audit Lab ablations to trim weak feature groups and optimize normalization per model.

lin_ftrl - 94%
1. Expose fully native distribution outputs from model-owned uncertainty instead of generic fallbacks.
2. Improve calibration drift handling by session/regime/horizon with stronger plugin-owned banks.
3. Refine replay hard-sample selection to focus on costly false positives and missed moves.
4. Add richer interaction uncertainty or sparse-feature reliability telemetry for the meta layer.
5. Use Audit Lab ablations to trim weak feature groups and optimize normalization per model.

lin_pa - 95%
1. Expose fully native distribution outputs from model-owned uncertainty instead of generic fallbacks.
2. Improve calibration drift handling by session/regime/horizon with stronger plugin-owned banks.
3. Refine replay hard-sample selection to focus on costly false positives and missed moves.
4. Add richer interaction uncertainty or sparse-feature reliability telemetry for the meta layer.
5. Use Audit Lab ablations to trim weak feature groups and optimize normalization per model.

lin_sgd - 93%
1. Expose fully native distribution outputs from model-owned uncertainty instead of generic fallbacks.
2. Improve calibration drift handling by session/regime/horizon with stronger plugin-owned banks.
3. Refine replay hard-sample selection to focus on costly false positives and missed moves.
4. Add richer interaction uncertainty or sparse-feature reliability telemetry for the meta layer.
5. Use Audit Lab ablations to trim weak feature groups and optimize normalization per model.

mem_retrdiff - 92%
1. Improve memory compaction and prototype diversity so retrieval stays informative under long runs.
2. Strengthen regime-aware similarity and confidence calibration from neighbor agreement.
3. Expose native distribution outputs directly from neighborhood statistics and uncertainty.
4. Use explicit window/context summaries in the embedding path for recurring motif detection.
5. Add Audit Lab motif-repeat and regime-shift scenarios targeted to retrieval behavior.

mix_loffm - 94%
1. Strengthen expert specialization and router discipline under noisy regime transitions.
2. Expose native distribution outputs using expert disagreement and calibrated uncertainty.
3. Reduce residual fallback dependence so EV is fully mixture-owned in live prediction.
4. Upgrade replay to balance rare but costly edge cases across experts.
5. Use Audit Lab to validate expert identity, skip discipline, and uncertainty rejection behavior.

mix_moe_conformal - 92%
1. Strengthen expert specialization and router discipline under noisy regime transitions.
2. Expose native distribution outputs using expert disagreement and calibrated uncertainty.
3. Reduce residual fallback dependence so EV is fully mixture-owned in live prediction.
4. Upgrade replay to balance rare but costly edge cases across experts.
5. Use Audit Lab to validate expert identity, skip discipline, and uncertainty rejection behavior.

rule_buyonly - 99%
1. No urgent change required; already effectively complete for its intended baseline role.

rule_m1sync - 96%
1. No urgent structural change needed; implementation is already close to full intended scope.
2. Keep Audit Lab coverage strong so rule behavior remains a trustworthy baseline.
3. Use these plugins as regression anchors for execution-path and gating changes.
4. Prefer deterministic explainability over additional complexity in this plugin family.
5. Only extend when a new baseline rule fills a real test gap in the framework.

rule_random - 99%
1. No urgent change required; already effectively complete for its intended baseline role.

rule_sellonly - 99%
1. No urgent change required; already effectively complete for its intended baseline role.

tree_catboost - 95%
1. Expose native leaf-distribution outputs with calibrated quantiles and confidence.
2. Strengthen skip-class treatment and uncertainty-aware leaf statistics in online updates.
3. Improve walk-forward stability with stricter drift-aware refresh and pruning logic.
4. Calibrate execution-grade EV directly from leaf distributions, not generic fallback behavior.
5. Use Audit Lab ablations to optimize schema, normalization, and context budgets per tree family.

tree_lgbm - 96%
1. Expose native leaf-distribution outputs with calibrated quantiles and confidence.
2. Strengthen skip-class treatment and uncertainty-aware leaf statistics in online updates.
3. Improve walk-forward stability with stricter drift-aware refresh and pruning logic.
4. Calibrate execution-grade EV directly from leaf distributions, not generic fallback behavior.
5. Use Audit Lab ablations to optimize schema, normalization, and context budgets per tree family.

tree_xgb - 93%
1. Expose native leaf-distribution outputs with calibrated quantiles and confidence.
2. Strengthen skip-class treatment and uncertainty-aware leaf statistics in online updates.
3. Improve walk-forward stability with stricter drift-aware refresh and pruning logic.
4. Calibrate execution-grade EV directly from leaf distributions, not generic fallback behavior.
5. Use Audit Lab ablations to optimize schema, normalization, and context budgets per tree family.

tree_xgb_fast - 94%
1. Expose native leaf-distribution outputs with calibrated quantiles and confidence.
2. Strengthen skip-class treatment and uncertainty-aware leaf statistics in online updates.
3. Improve walk-forward stability with stricter drift-aware refresh and pruning logic.
4. Calibrate execution-grade EV directly from leaf distributions, not generic fallback behavior.
5. Use Audit Lab ablations to optimize schema, normalization, and context budgets per tree family.

wm_cfx - 94%
1. Expose native distribution outputs from model-owned path uncertainty and downside statistics.
2. Use explicit window-aware state construction so short path structure is not inferred only from the latest bar.
3. Strengthen calibration under adverse-excursion and session-transition conditions.
4. Reduce any residual fallback/prior dependency so EV is truly world-model-owned.
5. Use Audit Lab market-replay packs to validate path quality, downside control, and session robustness.

wm_graph - 94%
1. Expose native distribution outputs from model-owned path uncertainty and downside statistics.
2. Use explicit window-aware state construction so short path structure is not inferred only from the latest bar.
3. Strengthen calibration under adverse-excursion and session-transition conditions.
4. Reduce any residual fallback/prior dependency so EV is truly world-model-owned.
5. Use Audit Lab market-replay packs to validate path quality, downside control, and session robustness.
